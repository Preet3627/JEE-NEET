
# -- Database Configuration --
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=jee_scheduler

# -- Server & Security --
PORT=3001
JWT_SECRET=change_this_to_a_random_secret_string_at_least_32_chars
ENCRYPTION_KEY=12345678901234567890123456789012 # Must be exactly 32 characters

# -- Google Integration --
# Get this from Google AI Studio (aistudio.google.com)
API_KEY=your_gemini_api_key
# Get these from Google Cloud Console (console.cloud.google.com)
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret

# -- Email Service (Optional) --
# Required for password resets/verification if not using Google Auth
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# -- Nextcloud / WebDAV Integration (Optional) --
# These are required for the "Study Material" and "Music Player" features.
# If these are not configured correctly, these features will be unavailable.

# 1. General Study Material (PDFs, Notes)
# Used for the "Study Material" tab
NEXTCLOUD_URL=https://cloud.example.com
NEXTCLOUD_SHARE_TOKEN=token_for_study_materials
NEXTCLOUD_SHARE_PASSWORD=password_for_study_materials_REQUIRED

# 2. Music Library (MP3s)
# Used for the "Music Player" widget
NEXTCLOUD_MUSIC_SHARE_TOKEN=token_for_music
NEXTCLOUD_MUSIC_SHARE_PASSWORD=password_for_music_REQUIRED
